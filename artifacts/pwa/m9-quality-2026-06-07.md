# M9 PWA Quality Gate

Captured: 2026-06-07

## Build Gates

```bash
bun --filter app lint
```

Result: PASS

```bash
cd apps/app && bunx tsc --noEmit
```

Result: PASS

```bash
NODE_OPTIONS=--max-old-space-size=8192 bun --filter app build
```

Result: PASS. Next.js 16.2.7 production build generated all `/es` and `/en`
static pages plus Node route handlers.

Root `turbo build` / `turbo lint` were killed immediately by the local Turbo
process wrapper in this environment, so M9 uses the app package gates above.

## Browser Smoke

Command path: in-app Browser against `bun --filter app start` on
`http://localhost:7001`.

Result: PASS. See `artifacts/pwa/m9-browser-smoke-2026-06-07.json`.

Screenshots:

- `artifacts/pwa/m9-home-desktop-2026-06-07.png`
- `artifacts/pwa/m9-home-mobile-2026-06-07.png`
- `artifacts/pwa/m9-home-en-mobile-2026-06-07.png`
- `artifacts/pwa/m9-wallet-prefill-mobile-2026-06-07.png`
- `artifacts/pwa/m9-panic-confirm-mobile-2026-06-07.png`
- `artifacts/pwa/m9-after-panic-home-mobile-2026-06-07.png`

Checks:

- Desktop home at 1280x720: no horizontal overflow.
- Mobile home at 390x844: no horizontal overflow.
- English route renders localized copy (`Hey you.`).
- Wallet `?invoice=` handoff is visible before seed generation and preserved in a
  read-only invoice field.
- Panic wipe confirmation modal opens, confirm button is unique, and confirming
  redirects back to `/es`.
- Browser read-only evaluation does not expose `localStorage`/`indexedDB`; code
  path was fixed to call both `wipeAll()` and `wipeAllFinance()`.

## PWA Surface

Manifest route returns:

- `display: standalone`
- `orientation: portrait`
- `theme_color: #0a0e14`
- PNG icons at 192x192 and 512x512 plus SVG icon

Service worker: `apps/app/public/sw.js` caches the app shell and never caches
`/api/*` inference/wallet/P2P calls.
